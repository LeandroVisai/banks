# Chatbot RAG — Banco Central
# Python 3.12 · CUDA 12.8 · H100
#
# Instalación:
#   pip install -r requirements.txt
#   pip install vllm --extra-index-url https://download.pytorch.org/whl/cu128
#
# Sin internet en el servidor:
#   pip download -r requirements.txt -d ./wheels/
#   pip install --no-index --find-links ./wheels/ -r requirements.txt

# ── LLM (cargado en proceso, sin HTTP) ─────────────────────────────────────────
vllm>=0.6.0
transformers>=4.46.0
sentencepiece>=0.2.0
tiktoken>=0.7.0

# ── API ────────────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.4.0

# ── Embeddings ─────────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0

# ── PostgreSQL (async pool) ────────────────────────────────────────────────────
psycopg[binary,pool]>=3.2.0
pgvector>=0.3.0

# ── Data Warehouse (Get_Data vía el módulo del servidor) ──────────────────────
pandas>=2.0.0
pyyaml>=6.0

# ── Utilidades ─────────────────────────────────────────────────────────────────
numpy>=1.26.0
