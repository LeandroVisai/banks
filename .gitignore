# ── Artefactos generados ──────────────────────────────────────────────────────
logs/
__pycache__/
*.pyc
*.pyo
*.pyd
.DS_Store

# ── Modelos (descargados desde HuggingFace o copiados al server) ──────────────
# Convención: models/<owner>--<name>/ para uso offline en H100
models/
models_cache/

# ── Pipeline de datos (generados por banks-ingest) ────────────────────────────
data/chunks*.json
data/documents*.json
data/images/
data/logs_intermedios/

# ── Logs de conversación del agente (JSONL diario) ───────────────────────────
data/chat_logs/

# ── Índice semántico del catálogo (regenerado desde el embedder en runtime) ───
data/catalog_index.npz

# ── Variables de entorno locales ──────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Python packaging ──────────────────────────────────────────────────────────
*.egg-info/
dist/
build/
.venv/
venv/

# ── Tests y cobertura ─────────────────────────────────────────────────────────
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# ── Reportes de evaluación (generados por make eval) ─────────────────────────
eval_report.md
eval_baseline.json

# ── IDEs ──────────────────────────────────────────────────────────────────────
.vscode/settings.local.json
.idea/
*.swp
*.swo

# TTS: bundle de wheels generado y reportes de noticias
jarvis_news_tts_wheels/
data/news_reports/
